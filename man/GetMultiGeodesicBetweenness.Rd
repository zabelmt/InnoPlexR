% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{GetMultiGeodesicBetweenness}
\alias{GetMultiGeodesicBetweenness}
\title{Multiplex geodesic betweenness centrality}
\usage{
GetMultiGeodesicBetweenness(
  Multiplex,
  normalized = TRUE,
  weighted = TRUE,
  omega = 1
)
}
\arguments{
\item{Multiplex}{A \code{Multiplex} object.}

\item{normalized}{Logical. Normalise betweenness scores? Default \code{TRUE}.}

\item{weighted}{Logical. Use edge weights (inverted)? Default \code{TRUE}.}

\item{omega}{Non-negative numeric. Inter-layer coupling strength. Default
\code{1}.}
}
\value{
A list with two elements:
\describe{
\item{\code{by_layer}}{Numeric matrix (\verb{nodes x layers}).}
\item{\code{aggregate}}{Named numeric vector normalised to \eqn{[0, 1]}.}
}
}
\description{
Computes shortest-path betweenness on the supra-graph using
\code{\link[igraph:betweenness]{igraph::betweenness()}}. Edge weights are inverted so that stronger
connections correspond to shorter distances.
}
\examples{
set.seed(1)
g1 <- igraph::sample_gnp(6, 0.5); igraph::V(g1)$name <- letters[1:6]
g2 <- igraph::sample_gnp(6, 0.5); igraph::V(g2)$name <- letters[1:6]
mx <- create_Multiplex(g1, g2, Layers_Name = c("A", "B"))
bc <- GetMultiGeodesicBetweenness(mx)
head(bc$aggregate)
}
