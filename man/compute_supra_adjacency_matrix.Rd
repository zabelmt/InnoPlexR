% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supra_matrices.R
\name{compute_supra_adjacency_matrix}
\alias{compute_supra_adjacency_matrix}
\title{Compute the supra-adjacency matrix of a multiplex network}
\usage{
compute_supra_adjacency_matrix(x, omega = 167)
}
\arguments{
\item{x}{A \code{Multiplex} object created by \code{\link[=create_Multiplex]{create_Multiplex()}}.}

\item{omega}{Non-negative numeric. Inter-layer coupling strength. Default
\code{167} (a strong coupling that keeps components connected).}
}
\value{
A sparse \code{dgCMatrix} of dimension \eqn{(N \cdot L) \times
  (N \cdot L)}, with row/column names of the form \code{"nodeName_layerIndex"}.
}
\description{
Builds the block \eqn{(N \cdot L) \times (N \cdot L)} supra-adjacency
matrix by stacking the per-layer adjacency matrices on the block diagonal
and filling the off-diagonal coupling blocks with the scalar \code{omega}.
}
\examples{
set.seed(1)
g1 <- igraph::sample_gnp(5, 0.5); igraph::V(g1)$name <- letters[1:5]
g2 <- igraph::sample_gnp(5, 0.5); igraph::V(g2)$name <- letters[1:5]
mx <- create_Multiplex(g1, g2, Layers_Name = c("A", "B"))
S  <- compute_supra_adjacency_matrix(mx, omega = 1)
dim(S)
}
