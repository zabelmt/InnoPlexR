% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{GetMultiPageRankCentrality}
\alias{GetMultiPageRankCentrality}
\title{Multiplex PageRank centrality}
\usage{
GetMultiPageRankCentrality(
  Multiplex,
  damping = 0.85,
  omega = 1,
  tol = 1e-06,
  max_iter = 100
)
}
\arguments{
\item{Multiplex}{A \code{Multiplex} object.}

\item{damping}{Numeric in \eqn{(0,1)}. PageRank damping factor. Default
\code{0.85}.}

\item{omega}{Non-negative numeric. Inter-layer coupling strength. Default
\code{1}.}

\item{tol}{Convergence tolerance for power iteration. Default \code{1e-6}.}

\item{max_iter}{Maximum number of power-iteration steps. Default \code{100}.}
}
\value{
A list with two elements:
\describe{
\item{\code{by_layer}}{Numeric matrix (\verb{nodes x layers}).}
\item{\code{aggregate}}{Named numeric vector normalised to \eqn{[0, 1]}.}
}
}
\description{
Runs power iteration on the damped supra-transition matrix to compute
multilayer PageRank scores.
}
\examples{
set.seed(1)
g1 <- igraph::sample_gnp(8, 0.4); igraph::V(g1)$name <- paste0("n", 1:8)
g2 <- igraph::sample_gnp(8, 0.4); igraph::V(g2)$name <- paste0("n", 1:8)
mx <- create_Multiplex(g1, g2, Layers_Name = c("A", "B"))
pr <- GetMultiPageRankCentrality(mx)
head(pr$aggregate)
}
