% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structural.R
\name{GetLocalClustering}
\alias{GetLocalClustering}
\title{Local clustering coefficient for multiplex networks}
\usage{
GetLocalClustering(SupraAdjacencyMatrix, Layers, Nodes)
}
\arguments{
\item{SupraAdjacencyMatrix}{A square sparse \code{dgCMatrix} as returned by
\code{\link[=compute_supra_adjacency_matrix]{compute_supra_adjacency_matrix()}}.}

\item{Layers}{Integer. Number of layers.}

\item{Nodes}{Integer. Number of nodes per layer.}
}
\value{
A single-column numeric matrix of length \code{Nodes} with clustering
coefficients in \eqn{[0, 1]}.
}
\description{
Computes the multiplex local clustering coefficient for each actor by
aggregating triangle counts across all layer pairs of the
supra-adjacency matrix using the block-tensor formulation.
}
\details{
Requires \code{muxViz::SupraAdjacencyToBlockTensor()} to decompose the
\eqn{(NL \times NL)} supra-adjacency matrix into its
\eqn{L^2} blocks. Install muxViz with
\code{remotes::install_github("manlius/muxViz")} if not already available.
}
\examples{
\donttest{
set.seed(1)
g1 <- igraph::sample_gnp(6, 0.6); igraph::V(g1)$name <- letters[1:6]
g2 <- igraph::sample_gnp(6, 0.6); igraph::V(g2)$name <- letters[1:6]
mx <- create_Multiplex(g1, g2, Layers_Name = c("A", "B"))
S  <- compute_supra_adjacency_matrix(mx, omega = 1)
cl <- GetLocalClustering(S, Layers = 2, Nodes = 6)
}
}
