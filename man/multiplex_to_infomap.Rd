% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structural.R
\name{multiplex_to_infomap}
\alias{multiplex_to_infomap}
\title{Convert a Multiplex object to Infomap edge-list format}
\usage{
multiplex_to_infomap(x, omega = 1, weight_col = TRUE)
}
\arguments{
\item{x}{A \code{Multiplex} object.}

\item{omega}{Non-negative numeric. Inter-layer coupling weight. Default
\code{1}.}

\item{weight_col}{Logical. Include a \code{weight} column in the output?
Default \code{TRUE}.}
}
\value{
A list with two data frames:
\describe{
\item{\code{edge_list}}{Columns \code{layer_from}, \code{node_from}, \code{layer_to},
\code{node_to}, and (optionally) \code{weight}.}
\item{\code{node_lookup}}{Columns \code{node_id} (integer) and \code{node_name}
(character).}
}
}
\description{
Produces the inter- and intra-layer edge list required by the Infomap
community-detection algorithm, together with a node look-up table mapping
integer IDs back to original node names.
}
\examples{
set.seed(1)
g1 <- igraph::sample_gnp(5, 0.5); igraph::V(g1)$name <- letters[1:5]
g2 <- igraph::sample_gnp(5, 0.5); igraph::V(g2)$name <- letters[1:5]
mx  <- create_Multiplex(g1, g2, Layers_Name = c("A", "B"))
inf <- multiplex_to_infomap(mx)
head(inf$edge_list)
}
