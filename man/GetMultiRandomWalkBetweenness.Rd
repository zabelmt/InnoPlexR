% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{GetMultiRandomWalkBetweenness}
\alias{GetMultiRandomWalkBetweenness}
\title{Multiplex random-walk betweenness centrality (Monte Carlo)}
\usage{
GetMultiRandomWalkBetweenness(
  Multiplex,
  tol = 1e-06,
  normalized = TRUE,
  n_samples = 1000,
  seed = 42
)
}
\arguments{
\item{Multiplex}{A \code{Multiplex} object.}

\item{tol}{Numeric. Eigenvalue threshold for truncating the pseudoinverse.
Default \code{1e-6}.}

\item{normalized}{Logical. Reserved for interface consistency; currently
scores are always normalised to \eqn{[0, 1]}.}

\item{n_samples}{Integer. Number of random source–target pairs to sample.
Larger values give more accurate estimates at higher cost. Default
\code{1000}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{42}.}
}
\value{
A list with four elements:
\describe{
\item{\code{by_layer}}{Numeric matrix (\verb{nodes x layers}) of raw scores.}
\item{\code{aggregate}}{Named numeric vector normalised to \eqn{[0, 1]}.}
\item{\code{n_samples}}{The number of pairs that were sampled.}
\item{\code{total_pairs}}{Total possible pairs \eqn{N(N-1)/2} (for context).}
}
}
\description{
Approximates current-flow (random-walk) betweenness via a Monte Carlo
approach: the pseudoinverse of the supra-Laplacian is computed once, then
node voltages are solved for a random sample of source–target pairs. This
is far more scalable than the exact \eqn{O(N^3)} computation.
}
\examples{
\donttest{
set.seed(1)
g1 <- igraph::sample_gnp(8, 0.4); igraph::V(g1)$name <- paste0("n", 1:8)
g2 <- igraph::sample_gnp(8, 0.4); igraph::V(g2)$name <- paste0("n", 1:8)
mx  <- create_Multiplex(g1, g2, Layers_Name = c("A", "B"))
rwb <- GetMultiRandomWalkBetweenness(mx, n_samples = 100)
head(rwb$aggregate)
}
}
